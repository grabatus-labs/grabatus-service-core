# Copy to terraform.tfvars and fill in real values. Never commit
# terraform.tfvars itself — .gitignore excludes it.

gcp_project = "grabatus-staging"
gcp_region  = "us-east1"
environment = "staging"
