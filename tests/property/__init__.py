"""Property-based tests using hypothesis.

These tests assert invariants over generated inputs rather than
specific examples. They complement the example-based unit tests
in ``tests/unit/`` and run on every push.
"""
